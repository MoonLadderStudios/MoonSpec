description = "Reconcile canonical docs after verified MoonSpec implementation."

prompt = """
Use the `moonspec-doc-reconcile` skill after `moonspec-verify` returns `FULLY_IMPLEMENTED`.

User input:
{{args}}

Use MoonSpec skill IDs and command IDs only: `moonspec-*` and `/moonspec.*`.
"""
