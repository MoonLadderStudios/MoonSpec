description = "Verify a completed MoonSpec implementation."

prompt = """
Use the `moonspec-verify` skill to perform final read-only verification for the active MoonSpec feature.

User input:
{{args}}

Use MoonSpec skill IDs and command IDs only: `moonspec-*` and `/moonspec.*`.
"""
